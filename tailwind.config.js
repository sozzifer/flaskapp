/** @type {import('tailwindcss').Config} */
module.exports = {
  // mode: 'jit': enable just-in-time mode
  mode: 'jit', 
  content: ["./templates/**/*.{html,htm}"],
  theme: {
    extend: {},
  },
  plugins: [],
}

